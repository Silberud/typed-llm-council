# Dependabot — automated dependency-update PRs.
# https://docs.github.com/en/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file
version: 2

updates:
  # Python deps declared in pyproject.toml
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
    commit-message:
      prefix: "deps"
    # Group security and version updates separately so a routine bump
    # PR doesn't drown a real security advisory.
    groups:
      python-runtime:
        patterns:
          - "pydantic*"
          - "httpx"
          - "tomli*"
      python-dev:
        patterns:
          - "pytest*"
          - "hypothesis"
          - "ruff"

  # GitHub Actions versions pinned in .github/workflows/
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 3
    labels:
      - "dependencies"
      - "ci"
    commit-message:
      prefix: "ci"
